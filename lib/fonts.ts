import { Poppins, Yantramanav } from 'next/font/google';

export const poppins = Poppins({
	subsets: ['latin'],
	weight: '400',
});

export const yanFont900 = Yantramanav({ weight: '900', subsets: ['latin'] });
